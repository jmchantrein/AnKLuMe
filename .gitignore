# Python
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/

# Live ISO test screenshots (generated, not tracked)
tests/live-iso/screenshots/
.hypothesis/

# Ansible
*.retry
.ansible/

# Molecule
.molecule/
.cache/

# Editor
*.swp
*.swo
*~
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# ── User-specific infrastructure ────────────────────────────
#
# The framework repo ships infra.yml.example as a template.
# Users create their own infra.yml and run `make sync` to generate
# inventory/, group_vars/, and host_vars/ files.
# These contain real IPs, subnet layouts, and machine names specific
# to each user's infrastructure — they must NOT be pushed to the
# public framework repository.
#
# If you are developing the framework itself and need to commit
# generated test fixtures, use: git add --force <file>
/infra.yml
/infra/
/inventory/
/group_vars/
/host_vars/

# User custom roles (override framework roles)
/roles_custom/

# Galaxy vendor roles (installed by make init from requirements.yml)
/roles_vendor/

# Instance backups (generated by make backup)
/backups/

# AI session logs
/logs/

# Code analysis reports (generated by make code-graph)
/reports/

# Desktop integration configs (generated by make desktop-config)
/desktop/

# AI config with secrets
/anklume.conf.yml

# MCP config (contains user-specific IPs)
/.mcp.json

# anklume dev mode marker (bypass pre-commit hook)
.anklume-no-verify

# GitHub token (never commit)
.github-token

# Scenario test backups (crash recovery)
.scenario-session-backup/
.scenario-backup/

# Agent personality (never tracked — ADR-036 reproducibility constraint)
SOUL.md

# Package manager lockfiles (uv)
uv.lock

# Live ISO images (large, generated by build-image)
*.iso
*.iso.sha256
images/

# Temporary dev artifacts
temp.txt
watch_*
control
control.tar.xz
*.bak

# Documentation site build output
site/
