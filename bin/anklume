#!/usr/bin/env python3
"""anklume CLI entry point."""
import sys
from pathlib import Path

# Add project root to path so 'scripts.cli' is importable
sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

from scripts.cli import app

app()
