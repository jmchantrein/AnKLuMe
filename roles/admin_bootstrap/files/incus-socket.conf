# Ensure /run/incus directory exists for proxy device socket (ADR-019)
# tmpfiles.d runs earlier than systemd services, avoiding the race
# condition where the proxy device binds before the directory exists.
d /run/incus 0755 root root - -
