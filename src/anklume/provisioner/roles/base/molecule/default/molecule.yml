---
driver:
  name: default
platforms:
  - name: molecule-base
provisioner:
  name: ansible
  env:
    ANSIBLE_ROLES_PATH: "${MOLECULE_PROJECT_DIRECTORY}/.."
  inventory:
    host_vars:
      molecule-base:
        ansible_connection: community.general.incus
verifier:
  name: ansible
