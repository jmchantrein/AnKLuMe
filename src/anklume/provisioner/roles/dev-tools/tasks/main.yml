# Rôle dev-tools — outils de développement
---
- name: Installer les outils de développement
  ansible.builtin.apt:
    name: "{{ dev_tools_packages }}"
    state: present
    update_cache: true
  tags: [install]
