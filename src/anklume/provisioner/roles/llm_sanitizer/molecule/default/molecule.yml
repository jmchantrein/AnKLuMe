---
driver:
  name: default
platforms:
  - name: molecule-llm-sanitizer
provisioner:
  name: ansible
  env:
    ANSIBLE_ROLES_PATH: "${MOLECULE_PROJECT_DIRECTORY}/.."
  inventory:
    host_vars:
      molecule-llm-sanitizer:
        ansible_connection: community.general.incus
verifier:
  name: ansible
