---
driver:
  name: default
platforms:
  - name: molecule-ollama-server
provisioner:
  name: ansible
  env:
    ANSIBLE_ROLES_PATH: "${MOLECULE_PROJECT_DIRECTORY}/.."
  inventory:
    host_vars:
      molecule-ollama-server:
        ansible_connection: community.general.incus
verifier:
  name: ansible
