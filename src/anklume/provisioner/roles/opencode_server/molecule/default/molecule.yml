---
driver:
  name: default
platforms:
  - name: molecule-opencode-server
provisioner:
  name: ansible
  env:
    ANSIBLE_ROLES_PATH: "${MOLECULE_PROJECT_DIRECTORY}/.."
  inventory:
    host_vars:
      molecule-opencode-server:
        ansible_connection: community.general.incus
verifier:
  name: ansible
